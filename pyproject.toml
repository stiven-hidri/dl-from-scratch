[project]
name = "dl-from-scratch"
version = "0.1.0"
description = "A personal collection of deep learning concepts implemented from scratch, for learning purposes."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = ["micrograd", "makemore"]

[dependency-groups]
dev = [
    "ruff>=0.16.3",
    "ty>=0.0.72",
]
